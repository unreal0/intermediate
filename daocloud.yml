version: 2.0

test:
    image:
        meteorhacks/meteord

    services:
        #- mongo

    # using default docker-link env
    env:
        #- MONGO_URL=mongodb://u7DZw3XibjLz94JG:p4OmQKFSBxsy2NuU8@10.10.72.139:27017/JOQGwA8XdrNuzmKU
        #- ROOT_URL=http://unreal-cooks1.daoapp.io
    install:
        - 

    before_script:
        - 
        - 

    script:


build:
    lite_image:
        compile:
            dockerfile_path: Dockerfile
            build_dir: /
            cache: true

        extract:
            - 

        package:
              dockerfile_path: Dockerfile.sec
              build_dir: /
              cache: true
